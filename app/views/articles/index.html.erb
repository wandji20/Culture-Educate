<div class="mt-1  w-100">
  <div class=" most-popular">
    <article class="">
      
        <%= image_tag @most_popular.image_url, class: 'image' if @most_popular.image %>

      <% if @most_popular && @most_popular.categories.first %>
        
        <h4> <%= link_to @most_popular.categories.first.name, @most_popular.categories.first %></h4>
        <h5><%= @most_popular.title %></h5>
      <% end %>    
    </article>
    <div class="side-content">
    <p>something extra I like</p>
    </div>
  </div>
  <section class="top-category">
    <% @categories.each do |category| %>
      <article class="">
        
        <% if category.articles.order(:created_at).first %>
          <%= image_tag category.articles.order(:created_at).last.image_url, class: 'image' if category.articles.order(:created_at).last.image %>
          <h5><%= link_to category.name, category %></h5>
          <h6><%= category.articles.order(:created_at).last.title %></h6>
        <% end %>
      
      </article>
    <% end %>
  </section>
</div>
